var mysql = require('mysql');

// Create a database connection and export it from this file.
// You will need to connect with the user "root", no password,
// and to the database "chat".

// dbConnection = mysql.createConnection({
//     user: 'root',
//     database: 'chat'
//   });
//   dbConnection.connect();

//   dbConnection.query('truncate ' + messagesTable, done);

// Establish database connection


/* Functions */
var addUser = (user) => {
    // Input: this function takes a username
    // Take username from param
    // Call mysql query function
    // Insert user into table
    // TEST: query for user in table

}
//dbConnection.query('SELECT * FROM users', done);